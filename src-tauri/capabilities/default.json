{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs (window, event, menu) plus the dialog plugin used by File > Open. App commands registered via invoke_handler (e.g. read_md_file) are reachable via the core IPC channel without per-command ACL entries.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
