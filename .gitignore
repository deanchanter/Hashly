.DS_Store
node_modules/

# Stale Rust build artifacts from the pre-remove-tauri-legacy era —
# the `src-tauri/` crate and root `Cargo.toml` are gone, so cargo no
# longer regenerates these. Listed here so existing local checkouts
# don't flood git status. Safe to delete with `rm -rf target/ src-tauri/`
# on any working copy.
target/
src-tauri/
dist/

# Pages Functions build output
dist-functions/
.wrangler/

# Local secrets for `wrangler pages dev`
.dev.vars

# ship-milestone skill scratch — local-only run log
specs/**/ship-milestone-scratch.md
.claude/.ship-milestone.*.scratch.md

# Playwright e2e (issue #159)
playwright/.cache/
test-results/
playwright-report/
